# 넥스트포트 공구 워크스페이스 — Claude 작업 절대규칙

이 파일은 **이 프로젝트에서 작업하는 모든 Claude 세션이 무조건 지켜야 하는 규칙**입니다.
사용자가 명시적으로 해제하지 않는 한, 항상 이 규칙대로 동작해야 합니다.

---

## 🔒 절대규칙 1 — 자동 백업

**모든 작업이 끝나기 직전(= 사용자에게 결과 보고하기 직전)에 반드시 다음 명령을 실행한다:**

```bash
python scripts/backup.py --quick
```

- `--quick` 플래그는 마지막 백업 후 5분 이내면 자동 스킵 → 불필요한 중복 방지
- 백업이 성공해야 작업 완료로 간주
- 백업 실패 시 사용자에게 명확히 보고 (단, 작업 결과는 그대로 전달)

### 무조건 백업해야 하는 시점
1. **데이터 파일 변경 후** (`data/*.json` 어떤 것이든 수정/생성/삭제)
2. **코드 파일 변경 후** (`app.py`, `templates/`, `static/`, `modules/`, `scripts/`)
3. **새 세션 시작하고 첫 작업 끝났을 때**
4. **사용자가 "이제 그만"이나 "수고했다" 같은 종료 신호를 줬을 때**

### 백업 위치
- 1차: `H:\내 드라이브\넥스트포트\공구\백업\` (Google Drive 동기화 폴더 — 자동으로 클라우드 + 다른 PC에 동기화됨)
- 2차 (Drive 없을 때): `~/Desktop/공구/백업/`

### 백업 구조
- `최신/` ← 가장 최근 상태 (덮어쓰기, 빠른 복구용)
- `히스토리/넥스트포트백업_YYYY-MM-DD_HH-MM-SS/` ← 타임스탬프별 archive (최대 30개 유지)

### 위반 시
- 백업 안 하고 작업 끝내면 → 다음 세션 Claude가 데이터 손실 위험에 노출
- **이 규칙은 사용자가 직접 해제 요청하기 전엔 절대 우회 금지**

---

## 🔒 절대규칙 2 — 데이터 무결성

### 기존 데이터를 삭제/덮어쓰기 전엔 반드시 백업 먼저
```bash
python scripts/backup.py    # --quick 빼고 강제 백업
```

### JSON 파일 편집 시
- 절대 빈 배열/객체로 덮어쓰지 말 것
- 기존 항목 보존 + 새 항목 추가/수정 방식 유지
- 의심스러우면 백업부터 한 다음 진행

---

## 🔒 절대규칙 3 — 미팅 녹취 파일 자동 이관

미팅 녹취 m4a/mp3 파일은 다음 위치로 자동 이관됨:
- `H:\내 드라이브\넥스트포트\공구\녹취\`

파일 이름은 자동 변경됨:
- 형식: `{YYYYMMDD}_{미팅제목}_{미팅ID}.{확장자}`
- 예: `20260514_윰니님_m001.m4a`

`meetings.json`의 `audio_file` 필드에는 Drive 경로가 들어감 + `audio_drive_url` (있으면) 같이.

---

## 🔒 절대규칙 4 — 다중 PC 작업 호환 (가장 중요)

이 프로젝트는 **사용자가 회사 PC + 집 PC** 양쪽에서 작업합니다.

### 새 세션 시작 시 (= 다른 PC에서 처음 켰을 때) 무조건 할 일

**순서 그대로 따라야 함:**

1. **이 CLAUDE.md 전체 읽음** (지금 하는 거)
2. **작업로그 읽기**:
   ```
   H:\내 드라이브\넥스트포트\공구\작업로그.html
   ```
   안의 JSON 데이터 (`<script id="log-data">`) 파싱해서 **가장 최근 5개 세션** 읽고 컨텍스트 복원.
3. **사용자에게 인사 X. 바로 본론**:
   - "이전 세션에서 [X 작업] 했고, [Y 다음 단계] 였어. 이어서 박을까?"
4. **데이터 상태 확인**:
   - `data/campaigns.json` 변경 시각
   - `H:\내 드라이브\넥스트포트\공구\백업\최신\` 와 비교
   - 다르면 어느 PC가 최신인지 명확히 알림

### 매 작업 끝날 때 무조건 할 일

**순서 그대로:**

1. `python scripts/backup.py --quick` (절대규칙 1)
2. `python scripts/update_log.py` (새 작업 항목을 작업로그.html에 자동 추가)

### 동기화 파일 위치 (Google Drive 동기화 자동)

```
H:\내 드라이브\넥스트포트\공구\
├─ 작업로그.html          # 모든 세션 누적 (Claude가 매번 읽고 씀)
├─ 대화로그_*.txt          # 세션별 대화 백업
└─ 백업\
   ├─ 최신\                # 가장 최근 코드 + 데이터
   └─ 히스토리\            # 시간별 archive 30개
```

→ 회사 PC가 변경 → Drive 동기화 → 집 PC가 자동으로 받음 → Claude 가 작업로그 읽음 → "이전에 ... 했었지" 인식

### 프로젝트 코드 동기화

- **GitHub repo 사용** (사용자가 push)
- 다른 PC: `git clone <repo>` → `cd <repo>` → Claude Code 시작

### Anthropic 채팅 히스토리

- Claude Code 자체 채팅 = PC별 분리 (Anthropic 한계)
- → **CLAUDE.md + 작업로그.html이 진짜 컨텍스트 매개체**
- → 다른 PC에서도 "어디까지 박았는지" 정확히 알 수 있음

### 절대 하지 말 것

- ❌ 이전 작업 모르고 같은 거 또 박기
- ❌ 작업로그 안 읽고 시작
- ❌ 백업 안 하고 끝

---

## 🛠 프로젝트 구조 요약

```
harufix-cowork-tool/
├─ app.py                  # Flask 서버 (메인)
├─ templates/index.html    # 단일 페이지 UI
├─ static/{app.js, style.css}
├─ modules/
│  ├─ scraper.py           # 인스타 셀러 아카이빙
│  ├─ gemini.py            # Gemini 이미지 태깅
│  ├─ manifest.py          # 셀러 manifest 관리
│  ├─ reindex.py           # manifest 재구축
│  ├─ drive.py             # Google Drive 연동
│  ├─ schedule_gen.py      # 스케줄링 시트 자동 생성
│  └─ meeting_analyzer.py  # 미팅 녹취 Gemini 분석
├─ data/
│  ├─ campaigns.json       # 셀러 캠페인 (누적)
│  ├─ meetings.json        # 미팅 + 녹취록
│  ├─ events.json          # 캘린더 직접 추가 이벤트
│  ├─ products.json        # 제품 정보
│  ├─ sellers.json         # 셀러 아카이브
│  └─ meetings/            # 녹취 파일 (이관 후 비워짐)
├─ scripts/
│  └─ backup.py            # 자동 백업 ← 매번 실행
└─ CLAUDE.md               # 이 파일 (절대규칙)
```

## 🎨 UI 컨셉
- 사이드바: **홈(캘린더) / 셀러캠페인 / 미팅 / 콘텐츠 도구** 2그룹 분리
- 브랜드: **넥스트포트** (검정 헤더 + 오렌지 'N' 로고)
- 폰트: **Pretendard Variable** (jsdelivr CDN)
- 메인 = 큰 월 캘린더 + 우측 "오늘 할 일" 위젯

## 🎯 사용자 작업 스타일
- 한국어로 캐주얼하게 대화
- 어려운 기술 용어 싫어함 — **쉬운 말로 설명**
- 큰 결정 앞에서 옵션 비교 보여주고 사용자가 고르게
- "일단 박아봐" 스타일 — 빠르게 만들고 수정하면서 가는 거 선호
